#!/bin/bash

echo '=== Fix Guide Started ==='


# Running self-maintenance
python3 -c 'from modules.self_maintenance import SelfMaintenance; from niblit_memory import LocalDB; db=LocalDB(); SelfMaintenance(db).run()'

# Running self-healer
python3 -c 'from modules.self_healer import SelfHealer; from niblit_memory import LocalDB; db=LocalDB(); SelfHealer(db).repair()'

echo '=== Fix Guide Completed ==='