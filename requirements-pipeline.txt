# requirements-pipeline.txt
# ──────────────────────────────────────────────────────────────────────────────
# Optional dependencies for niblit_full_upgrade_pipeline.py
# Install with:  pip install -r requirements-pipeline.txt
#
# All of these are OPTIONAL — the pipeline falls back gracefully to built-in
# alternatives (in-memory vector search, SQLite graph, subprocess sandbox, etc.)
# when any of these packages are absent.
# ──────────────────────────────────────────────────────────────────────────────

# Docker SDK — enables Docker sandbox for isolated code execution.
# Requires Docker Desktop or Docker Engine on the host.
docker>=6.0.0

# Neo4j driver — enables graph database relationship mapping.
# Requires a running Neo4j instance (cloud or self-hosted).
py2neo>=2021.2.4

# Prometheus metrics client — exposes /metrics endpoint for Grafana.
prometheus-client>=0.19.0

# Vector search (FAISS) — high-performance semantic similarity search.
# Automatically used by modules/vector_store.py when installed.
faiss-cpu>=1.7.4

# Sentence-transformers — local embedding generation for vector search.
# Used by modules/vector_store.py when FAISS or Qdrant is enabled.
sentence-transformers>=2.2.2

# numpy — required by faiss-cpu and sentence-transformers.
numpy>=1.24.0
